import { Routes } from '@angular/router';
// app
import { FirebaseFilteringComponent } from './firebase-filtering/firebase-filtering.component';

export const FirebaseFilteringRoutes: Routes = [
    {
        path: '',
        component: FirebaseFilteringComponent
    }
];
